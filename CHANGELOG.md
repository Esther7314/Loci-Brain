# CHANGELOG

## 1.2.0 · 2026-08-20

> 为什么是「次」不是「补丁」：底下**三处别人会撞到的行为变了**（hook 路由要钥匙、
> `regrow` 不再默默吞掉两个参数、网关只转发 `/v1/*`）。
> 判据：**版本号是给读的人用的 —— 1.1.1 等于说「你用的方式一个字没变」，这句话不真就别写。**

### 🔴 安全：四条 hook 路由不再是「免检」

`/api/loci/poke` · `/api/muse/pending` · `/api/dream/current` ·
`POST /api/loci/dream/wake` 这四条以前在免检名单里 —— **设了面板密码也拦不住**，
既读得到梦的正文，又改得了状态（`dream/wake` 会动生命周期和回想次数）。
名单里的理由是「调用方是桥，它没有 cookie」：**理由成立，解法错了**。

现在的规矩：**门锁了就没有例外。**

| 你的情况 | 会发生什么 |
|---|---|
| **没设面板密码**（默认） | **什么都不用改**，一切照旧 |
| 设了面板密码 + 用桥 | 要配一把钥匙：`LOCI_HOOK_TOKEN=…`，桥在请求头 `x-loci-hook-token` 里带上 |
| 设了面板密码 + 没配钥匙 | 这四条**直接拒绝**（fail-closed），401 里会说清楚要配什么 |

⚠️ 钥匙走请求头，**不放查询串** —— 地址栏会被日志 / Referer / 浏览器历史带出去。
（自带的 `gateway/` 已经会带；`LOCI_HOOK_TOKEN` 没设时行为不变。）

### 🔴 装得上、跑得起来（这两条以前只有我们自己没事）

- **`requirements.txt` 加了上限 `mcp>=1.0.0,<2`。** 原来写的是 `>=1.0.0`，
  而 mcp 2.0 把 `mcp.server.fastmcp` 整个挪走了 —— 照着装的人**一启动就报
  `No module named 'mcp.server.fastmcp'`**。我们没撞上是因为我们装 lock 文件。
- **`docker-compose.yml` 默认打开 `build: .`。** 原来它是注释掉的，而
  `loci-brain:latest` 没发布到任何 registry —— 干净机器上 `docker compose up -d`
  只会得到一句「找不到镜像」。

### 变了（升级前看一眼）

- **`regrow` 不再接受 `when` / `room`**（元数据归 `trace`）。
  这两个参数 8-19 就撤了，但 `regrow` 是当时唯一没装「拒绝不认识的参数」那道闸的工具 ——
  **传了既不报错也不生效**。现在会当场报错。
  （顺带：九个工具**全部**装上了这道闸，改成遍历工具表自动装，不再靠人记名单。）
- **`trace` 新收 `room` / `when` / `folds_append`**（8-19 加的，这一版一并发布）。
  轴是：**要改它说的话 → `regrow`；要改它是什么、挂在哪天 → `trace`**。
- **做梦不再把 want 的 `weight` 清零。** 改成记一笔 `last_dreamt`，选料时对刚梦过的
  打折。原来那个做法把两件事混成了一件（「别老做同一个梦」和「它不压着我了」），
  而后果是反的：**最放不下的那件，被梦到一次就从「压在心头」里消失了。**
- **「时间涟漪」删了。** 它每次 touch 都扫一遍全库，去给时间相邻的桶
  加一个 `activation_count` —— 而遗忘公式 8-16 起就不看那个数了。
- **自带的 `gateway/` 只转发 `/v1/*`**，别的路径本地 404。
  原来它什么路径都往上游转（`/favicon.ico`、打错的地址、扫描器探针，
  全都带着你的 Authorization 转出去）。
- **`_bigevent.covering()` 改签名**：`covering(buckets, t0, t1)` —— 桶由调用方递进来，
  不再自己去拿全局。（它同时不再是 `async`。）

### 修了

- **网关：上游 gzip 时回应被截断。** 转发响应头时只跳过了 `content-encoding`，
  `content-length` 照抄 —— 而那是**压缩后**的长度。非流式请求因此拿到半截 JSON。
  流式请求不受影响（chunked，没有这个头），所以它一直没被发现。
- **日历上不存在的日子会让代码抛异常。** `2026-09-31` 这类「形状对、日子不存在」的
  输入，写口收得下、读路会崩 —— **一次填错能让那段时间的记忆读不出来。**
  写口现在当场拒（「日历上没有 2026-09-31 这一天」），读路挡漏了退兜底、不炸。
- **`update()` 静默丢弃不在白名单里的字段** —— 这一版补了 `last_dreamt`。
  （这个坑在同一段注释里已经写过一次，仍然又踩了；现在由一条会变红的断言守着。）
- 睁眼那一屏的说明改成了实话：**准则那一格逐字印原文，「提醒」和「忽然想起」
  两格是模型写的摘要。** 原来那句「这一屏一个字都不过模型」对后两格不成立。

### 新增

- **`/health`（自带 gateway）**：回答「**它最近一次真的干成活是什么时候**」，
  不是「进程还在吗」。能认出「触发了 N 次、一次都没贴上」这种**安静地什么都不做**的状态。
- **体检 `/api/loci/pulse` 加了「它在不在工作」一段**：打标 / 向量 / 做梦各自
  最近一次成功是多久以前，还有多少条在排队。
- **测试随仓库交付了**：`tests/`（118 条纯函数单元测试，0.3 秒）+
  `gateway/tests/`（19 条，假上游 + 假 Loci，一条真连接都不出门）。
  一条命令：`python scripts/quick_check.py`（lint + 单元 + 网关，约 6 秒）。
- **CI**：每次 push / PR 自动跑上面那条命令。
  ⚠️ 它跑不了端到端那套（要真容器 + 真模型）—— **绿了只代表语法和单元层没问题。**

## 1.1.0 · 2026-08-19

### 🔴 修掉一个已经推出去的崩溃（如果你在 8-19 白天 clone 过，请更新）

`tools/trace/core.py` 里有两个未定义名（`why_remembered` / `requested_importance`），
**任何走到那段的 `trace` 调用都会当场 NameError**。
原因是一次误推：砍死参数砍到一半的工作树被 `git add -A` 卷进了另一条 commit。
（教训写在这儿供人一笑：**手上有活就先落一条 commit，别把半成品留在暂存区。**）

### 变快

- **`recall` 浏览（不带 `query`）13 秒 → 0.014 秒**。两个原因，都在 `bucket_manager`：
  - 「上次检查文件是什么时候」记的是**开扫之前**的时刻，而扫一遍 1000 个文件在
    Windows 的 bind mount 上要 2.6 秒 > 间隔 1 秒 —— 于是**每次调用都判定过期、
    每次都重扫，缓存一次都没命中过**
  - 文件指纹改走 `os.scandir`（目录列举本来就带 mtime/size，不必再 `os.stat` 一遍）：
    1000 个文件 2.45 秒 → 1.05 秒
- **面板 `/api/loci/recall` 不再把同一次搜索算两遍**（原来 `recall_data()` 和
  `recall_core()` 各走一遍 `_collect`）。带 `query` 时 8.6 秒 → 约 3 秒。

### 变了（升级前看一眼）

- **`recall` 浏览面的长相**：
  - 「时期」不再用 `◈` 符号，直接写「时期」两个字；**一格只留一条**（原来最多 3 条）
  - **认知快照（gist）的标题行不在浏览面出现了**。判据：翻记忆的时候要看的是 event；
    认知在「突出的点」那儿露面。被折起的那些照旧算进统计、搜得到、id 直查得到
  - **`regrow` 换版的旧版不再算进条数**，也不再以「▣… 盖着这里 1 条」在原地出头。
    一条换了版还是一条。旧版没消失：id 直查逐字 + 版本链两头照旧
- **`trace` 删掉七个早已不可用的参数**（`importance` / `resolved` / `digested` /
  `content` / `why_remembered` / `meaning_append` / `meaning_replace`）。
  它们 8-18 就从工具面撤了（`extra="forbid"` 挡着），这次是把底下的死签名一起清掉。
  ⚠️ `importance` 作为**内部字段**没动：`pin` 一条准则仍然把它锁成 10。
- **`breath` 底下五支没有入口的检索整个删了**（`catalog` / `feel` / `importance` /
  `surface` / `search` + `_verbatim`，共 6 个文件）。工具面的 `breath` 8-18 起就是零参数的，
  这些分支永远走不到。找东西是 `recall` 的活。

### 撤掉

- **`gateway/近期记忆视图.js`**（连同 `LOCI_RECENT_VIEW`）。
  它做的是「隔天开窗，把 `recall(when="昨天")` 的原文摘录贴进去」，
  而「昨天做了什么」本来就该是**收窗时压出来的那几句**，第二天带着它开窗即可 ——
  同一件事的第二个做法，而且要多问 Loci 一次。思路留在 `gateway/README.md` 第四节。

### 加了

- **`RELEVANCE_STRONG_WORDS`**：强档词表可以整表换（逗号分隔，或 `gateway/强档词.json`），
  匹配不分大小写。
  🔴 **为什么这条重要**：仓库带的是中文词表，而弱档默认关 ——
  **不说中文的人装上之后，相关记忆提醒一次都不会触发，而且不会收到任何提示。**
  跟「超时 5 秒」是同一种失败：安静地什么都不做。
- **对外函数的英文别名**：`paste` / `computeReminder` / `appendToTail`
  （＝`贴一次` / `算相关记忆提醒` / `贴到真尾巴`，同一个函数）。
- `gateway/README.md` 重写。

---

## 1.0.0 · 2026-08-19

首个公开版本。基于 [Ombre-Brain](https://github.com/P0luz/Ombre-Brain) 二次开发（MIT）。
